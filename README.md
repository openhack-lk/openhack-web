# openhack.lk

Open-source directory of hackathons happening across Sri Lanka. Static Astro site, mock data via Content Collections for now.

```sh
npm install
npm run dev      # localhost:4321
npm run build    # ./dist
```

Add a hackathon by dropping a JSON file into `src/content/hackathons/` matching the schema in `src/content.config.ts`.
